
  # Generar mockups de PWAs

  This is a code bundle for Generar mockups de PWAs. The original project is available at https://www.figma.com/design/4H2ncD8KTsut4131piMD9g/Generar-mockups-de-PWAs.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  